local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")

local LP = Players.LocalPlayer
local LOGO_ID = "rbxassetid://95263963174351"
local CHROME_ID = "rbxassetid://70522037210432"

local gui = Instance.new("ScreenGui")
gui.Name = "NullHubOutdated"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 2147483647
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = LP:WaitForChild("PlayerGui")

local dim = Instance.new("Frame", gui)
dim.Size = UDim2.fromScale(1, 1)
dim.BackgroundColor3 = Color3.new(0, 0, 0)
dim.BackgroundTransparency = 1
dim.BorderSizePixel = 0
TweenService:Create(dim, TweenInfo.new(0.4), { BackgroundTransparency = 0.55 }):Play()

local card = Instance.new("Frame", gui)
card.AnchorPoint = Vector2.new(0.5, 0.5)
card.Position = UDim2.fromScale(0.5, 0.5)
card.Size = UDim2.fromOffset(460, 260)
card.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
card.BorderSizePixel = 0
Instance.new("UICorner", card).CornerRadius = UDim.new(0, 14)

local stroke = Instance.new("UIStroke", card)
stroke.Color = Color3.fromRGB(60, 60, 60)
stroke.Thickness = 1

local logo = Instance.new("ImageLabel", card)
logo.Size = UDim2.fromOffset(64, 64)
logo.Position = UDim2.fromOffset(24, 24)
logo.BackgroundColor3 = Color3.new(0, 0, 0)
logo.BorderSizePixel = 0
logo.Image = LOGO_ID
logo.ScaleType = Enum.ScaleType.Fit
Instance.new("UICorner", logo).CornerRadius = UDim.new(0, 12)

local title = Instance.new("TextLabel", card)
title.Size = UDim2.new(1, -110, 0, 26)
title.Position = UDim2.fromOffset(102, 24)
title.BackgroundTransparency = 1
title.Text = "NULL HUB · OUTDATED"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left

local sub = Instance.new("TextLabel", card)
sub.Size = UDim2.new(1, -110, 0, 16)
sub.Position = UDim2.fromOffset(102, 50)
sub.BackgroundTransparency = 1
sub.Text = "This script is OUTDATED."
sub.TextColor3 = Color3.fromRGB(200, 200, 200)
sub.TextSize = 12
sub.Font = Enum.Font.Gotham
sub.TextXAlignment = Enum.TextXAlignment.Left

local line1 = Instance.new("Frame", card)
line1.Size = UDim2.new(1, -48, 0, 34)
line1.Position = UDim2.fromOffset(24, 100)
line1.BackgroundTransparency = 1

local prefix = Instance.new("TextLabel", line1)
prefix.Size = UDim2.fromOffset(100, 34)
prefix.Position = UDim2.fromOffset(0, 0)
prefix.BackgroundTransparency = 1
prefix.Text = "Check out v2 on"
prefix.TextColor3 = Color3.fromRGB(180, 180, 180)
prefix.TextSize = 13
prefix.Font = Enum.Font.Gotham
prefix.TextXAlignment = Enum.TextXAlignment.Left

local chrome = Instance.new("ImageLabel", line1)
chrome.Size = UDim2.fromOffset(20, 20)
chrome.Position = UDim2.fromOffset(104, 7)
chrome.BackgroundTransparency = 1
chrome.Image = CHROME_ID
chrome.ScaleType = Enum.ScaleType.Fit

local urlText = Instance.new("TextLabel", line1)
urlText.Size = UDim2.fromOffset(280, 34)
urlText.Position = UDim2.fromOffset(128, 0)
urlText.BackgroundTransparency = 1
urlText.Text = "null-hub.vercel.app"
urlText.TextColor3 = Color3.fromRGB(230, 230, 230)
urlText.TextSize = 13
urlText.Font = Enum.Font.GothamBold
urlText.TextXAlignment = Enum.TextXAlignment.Left

local body = Instance.new("TextLabel", card)
body.Size = UDim2.new(1, -48, 0, 70)
body.Position = UDim2.fromOffset(24, 140)
body.BackgroundTransparency = 1
body.Text = "Or check out my V2 showcase post on TikTok.\n" ..
            "Made with Love, Kris. @nullhubontop ON TIKTOK!"
body.TextColor3 = Color3.fromRGB(180, 180, 180)
body.TextSize = 13
body.Font = Enum.Font.Gotham
body.TextXAlignment = Enum.TextXAlignment.Left
body.TextYAlignment = Enum.TextYAlignment.Top
body.TextWrapped = true

local countdown = Instance.new("TextLabel", card)
countdown.Size = UDim2.new(1, -48, 0, 22)
countdown.Position = UDim2.new(0, 24, 1, -34)
countdown.BackgroundTransparency = 1
countdown.Text = "Kicking you now in 10..."
countdown.TextColor3 = Color3.fromRGB(240, 200, 90)
countdown.TextSize = 13
countdown.Font = Enum.Font.GothamBold
countdown.TextXAlignment = Enum.TextXAlignment.Left

local bar = Instance.new("Frame", card)
bar.Size = UDim2.new(1, -48, 0, 3)
bar.Position = UDim2.new(0, 24, 1, -8)
bar.BackgroundColor3 = Color3.new(1, 1, 1)
bar.BorderSizePixel = 0
Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)

task.spawn(function()
    local remaining = 10
    TweenService:Create(bar, TweenInfo.new(remaining, Enum.EasingStyle.Linear), {
        Size = UDim2.new(0, 0, 0, 3),
    }):Play()

    while remaining > 0 do
        countdown.Text = "Kicking you now in " .. remaining .. "..."
        task.wait(1)
        remaining = remaining - 1
    end

    countdown.Text = "Kicking you now..."

    task.wait(0.4)

    local fade = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
    TweenService:Create(dim, fade, { BackgroundTransparency = 1 }):Play()
    local out = TweenService:Create(card, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.fromOffset(0, 0),
    })
    out:Play()
    out.Completed:Connect(function()
        gui:Destroy()
        pcall(function()
            LP:Kick("This script is OUTDATED. Check null-hub.vercel.app or @nullhubontop on TikTok.")
        end)
    end)
end)
